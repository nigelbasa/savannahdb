#pragma once

// LMDB-backed IStorageBackend. Implemented in Phase 0.5 — the MemoryBackend
// defined in `memory.h` is swapped for this one without touching the rest of
// the engine, which is the whole point of the IStorageBackend boundary.

namespace savannah::storage {
// reserved for Phase 0.5
}  // namespace savannah::storage
