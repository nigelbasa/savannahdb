#pragma once

// Phase 0.2 will add a BSON builder for server-synthesised responses
// (cursor batches, aggregation stage output). Phase 0.1 only echoes
// buffers received from the TS wire layer, so no builder is needed yet.

namespace savannah::bson {
// reserved for Phase 0.2
}  // namespace savannah::bson
